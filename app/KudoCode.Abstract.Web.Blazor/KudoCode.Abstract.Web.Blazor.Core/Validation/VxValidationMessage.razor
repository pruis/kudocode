@namespace KudoCode.Abstract.Web.Blazor.Validation

@typeparam TValue
@inherits VxValidationMessageComponent<TValue>

@foreach (var message in ValidationMessages)
{

    <div class="@Class">
        @message
    </div>
}
